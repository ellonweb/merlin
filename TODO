update LICENSE in all files (some files don't have any license atm)
    add jester to the list of (C) names: a _lot_ of code is copy/pasted straight from munin

Channel access min/max system
    inc a way to check for a user but proceed even if user doesn't exist

reloading loadable is unsafe, needs fixing

Hooks.request
    scan requests
    !request p/au/etc x.y.z [dists]
    !request cancel id
    robocop interface
        msg #chan "ellonweb has requested a p scan of x:y:z. <url>"
    intel.dists
        fill in data from scan requests ("ellonweb informs that atleast n dists are needed")
        provide data when requesting a scan: "x:y:z had n dists at tick ..."

Hooks.scan
    scans ala munin
    robocop interface
    chanusertracker interface
        pm all known users that their scan is done
    intel.dists
        fill in data from completed scans

Hooks.blocked
    more manual info for intel.dists
    @require_access_level(access.get('scan',access['member']))

Quotes and slogans:
    addquote, 
    quote, 
    remquote, 
    addslogan, 
    slogan, 
    remslogan, 

Scans:
    au, 
    dev, 
    jgp, 
    news, 
    planet, 
    scans, 
    unit, 

Bookings:
    book, 
    status, 
    unbook, 

Access and Channels:
    alias, 
    addchan, 
    galchan, 
    remchan, 
    galmate, 

Intel:
    racism, 

Penis:

Simple calcs and lookups:
    bashee, 
    basher, 
    exile, 
    launch, 
    lookup, 
    maxcap, 
    roidcost, 
    roidsave, 
    xp, 

Growth:
    exp, 
    value, 

Target search:
    cunts, 
    idler, 
    victim, 
    whore, 

Cookies and Props:
    cookie, 
    gac, 
    prop, 
    yourmum, 

Defence:
    logdef, 
    mydef, 
    searchdef, 
    showdef, 
    usedef, 

SMS:
    showmethemoney, 
    sms, 
    smslog, 

afford, 
bcalc, 
surprisesex, 
topcunts, 
